import type { ThemeDefinition } from "vuetify";

const light: ThemeDefinition = {
  colors: {
    primary: "#ef6b29",
  },
};

const dark: ThemeDefinition = {
  colors: {
    primary: "#ef6b29",
  },
};

export default {
  themes: {
    light,
    dark,
  },
};
